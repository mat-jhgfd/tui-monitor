# How to use
use this to use the app
```shell
cargo run --release
```
or
```shell
cargo r -r
```
It's the same

# For documentation
There is documentation in the whole code.
Check well the src/app.rs documentation.
To view the documentation code ```///``` and ```//!``` better use 
```shell
cargo doc --open
```
